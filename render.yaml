services:
  - type: web
    name: sistema-isp-deploy
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn core.wsgi:application --bind 0.0.0.0:$PORT
    rootDir: sistema_isp/projeto_django_backup